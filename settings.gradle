plugins {
	id "org.gradle.toolchains.foojay-resolver-convention" version "1.0.0"
}

rootProject.name = "Shattered"

include "dawn_core"
project(":dawn_core").name = "core"

include "dawn_lib"
project(":dawn_lib").name = "lib"

include "dawn_app"
project(":dawn_app").name = "app"